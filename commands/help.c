#include <syscall.h>
#include <unistd.h>
#include <stdio.h>
#include <sys/types.h>
#include <stdlib.h>
#include <yas.h>

int help(int argc, char *argv[]) {
	/*int cd;
	char *cmds[] = {"alias bye cd printenv setenv unalias unsetenv"};
	//if cd has no arguments go to the home dir else go to the directort in the argument
	if(argc == 2) {
		cd = chdir(getenv("HOME"));
	}
	else {
		printf("Select a command to get help on:\n");
		while(cmds) {
			printf("%s\n");
			cmds++
		}
		getch();
	}
	*/
}